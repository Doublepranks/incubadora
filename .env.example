NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:4173

# Database (PostgreSQL)
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/dashboard?schema=public

# Auth / Security
AUTH_SECRET=changeme-super-secret
COOKIE_SECURE=false

# External integrations
APIFY_TOKEN=
APIFY_ACTOR_ID=
APIFY_RETRY_ACTOR_ID=
RETRY_ENABLED=true
RETRY_SCHEDULE_CRON= # opcional: defina um cron para rodar o retry via agenda; deixe vazio para não usar
ENABLE_SYNC_JOB=true
SYNC_CRON=0 3 * * MON

# Optional seed user (used by prisma/seed.ts)
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=changeme123
SEED_ADMIN_NAME=Admin User
